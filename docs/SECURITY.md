# Security

Security-sensitive boundaries for this project include raw local artifact handling,
archive-home storage, path redaction, and deterministic report generation. Tests must
remain network-free and fixtures must stay sanitized.
