# Future Work

Planned later work now sits beyond the v1 delivery layer:

- richer provider-specific ingestion beyond the currently supported local inputs
- broader pricing-rule coverage and future pricing planes
- deeper reconciliation against external reference exports
- optional share bundles and export packaging
- release execution after the current scaffolding is promoted from dry-run readiness to an actual release
