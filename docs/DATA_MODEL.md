# Data Model

The canonical v1 ledger is event-level and SQLite-backed. Phase 0 includes only the
`schema_migrations` bootstrap path so migration discipline exists before the full schema
is added in Phase 1.
